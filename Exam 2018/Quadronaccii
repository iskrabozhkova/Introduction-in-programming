#include <iostream>

int main() {
	int a, b, c, d;
	int n;
	std::cin >> a >> b >> c >> d >> n;

	do {
		int sum = a + b + c + d;
		a = b;
		b = c;
		c = d;
		d = sum;
		n--;

	} while (n > 4);


	std::cout << d;
	return 0;
}
